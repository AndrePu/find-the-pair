export class CardInfo {
    constructor(visible, chosen, chosenColor, pair) {
        this.visible = visible;
        this.chosen = chosen;
        this.chosenColor = chosenColor;
        this.pair = pair;
    }
}
